{
    "name": "bun-bench",
    "module": "index.ts",
    "type": "module",
    "private": true,
    "scripts": {
        "lint": "biome check",
        "build": "bun build ./src/index.ts --minify --outdir=./dist --target=bun && tsc"
    },
    "devDependencies": {
        "@biomejs/biome": "^2.3.11",
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5"
    }
}
